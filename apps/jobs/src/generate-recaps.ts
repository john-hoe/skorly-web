import type { Env } from "./env";

/**
 * Day 6 skeleton: for fixtures that just finished (~30min ago) without a recap,
 * generate recap + tactical articles through the QA pipeline.
 */
export async function generateRecaps(_env: Env): Promise<{ generated: number }> {
  // TODO Day 6: query recently finished fixtures, generate recap/tactical.
  return { generated: 0 };
}
